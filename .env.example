# Copy to .env at the repo root. Compose reads it automatically.
# .env is gitignored; this file is committed as documentation.

POSTGRES_USER=smarthire
POSTGRES_PASSWORD=smarthire
POSTGRES_DB=smarthire
