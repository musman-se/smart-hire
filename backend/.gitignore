# Virtualenv — rebuilt from pyproject.toml, never committed
.venv/
venv/

# Python bytecode
__pycache__/
*.py[cod]

# Secrets — .env.example is committed, .env is not
.env

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Build artifacts
*.egg-info/
dist/
build/
